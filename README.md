# PROYECTO SAGAZ - BACKEND


## Requirements
Built in Django 4.0.4 and Python 3.8
Frontend is built in Vue 3: 

## Installation
To install dependencies, first you must run the following command from the root folder:
`pip install requirements.txt`

